{
  "name": "xml-retrieval-scheduler-electron",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package-win": "electron-packager . \"XMLScheduler\" --platform=win32 --arch=x64 --out=release-builds --overwrite"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "electron": "^25.2.0",
    "electron-packager": "^17.1.2"
  }
}